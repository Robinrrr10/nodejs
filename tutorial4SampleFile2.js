module.exports.findcount = function(arr){
  return 'array length is: ' + arr.length;
}

module.exports.subtract = function(a, b) {
  return `sub of result is ${a-b}`
}

module.exports.username = "Ram";
